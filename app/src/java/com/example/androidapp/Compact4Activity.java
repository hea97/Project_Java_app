package com.example.app;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class Compact4 extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_compact4);

        // Example: Set dynamic content or behaviors
        // ImageView jpImage = findViewById(R.id.jp_image);
        // jpImage.setImageResource(R.drawable.jp);

        // Add logic as needed to handle user interactions or data updates.
    }
}
